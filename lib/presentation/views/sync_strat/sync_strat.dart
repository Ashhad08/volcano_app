import 'package:flutter/material.dart';

import 'layout/body.dart';
class SyncStratView extends StatelessWidget {
  const SyncStratView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SyncStratViewBody(),
    );
  }
}
